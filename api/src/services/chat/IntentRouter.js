const BookingIntentHandler = require('./handlers/BookingIntentHandler');const MenuIntentHandler = require('./handlers/MenuIntentHandler');const BranchIntentHandler = require('./handlers/BranchIntentHandler');const SearchIntentHandler = require('./handlers/SearchIntentHandler');const TakeawayIntentHandler = require('./handlers/TakeawayIntentHandler');const DefaultIntentHandler = require('./handlers/DefaultIntentHandler');class IntentRouter {    constructor() {        this.handlers = [            new BookingIntentHandler(),            new TakeawayIntentHandler(),             new MenuIntentHandler(),            new BranchIntentHandler(),            new SearchIntentHandler(),            new DefaultIntentHandler(),        ];    }    async route(payload) {        const { intent, context, metadata } = payload;        if (!context.message && payload.message) {            context.message = payload.message;        }        for (const handler of this.handlers) {            try {                if (handler.canHandle(intent, context, metadata)) {                    const result = await handler.handle(payload);                    if (result) {                        return result;                    }                }            } catch (error) {                }        }        return null;    }}module.exports = IntentRouter;