const BranchHandler = require('../BranchHandler');class BranchFormatter {    static async formatBranchListWithDetails(branches) {        if (!branches || branches.length === 0) {            return [];        }        return await Promise.all(branches.map(async (branch) => {            let districtName = '';            if (branch.district_id) {                const district = await BranchHandler.getDistrict(branch.district_id);                districtName = district ? district.name : '';            }            const address = branch.address_detail || '';            const phone = branch.phone || '';            const openingHours = branch.opening_hours ? `${branch.opening_hours}h` : '';            const closeHours = branch.close_hours ? `${branch.close_hours}h` : '';            const hours = openingHours && closeHours ? `${openingHours} - ${closeHours}` : (openingHours || closeHours || '');            let branchInfo = `🏢 ${branch.name}`;            if (address) branchInfo += `\n📍 ${address}`;            if (districtName) branchInfo += ` (${districtName})`;            if (phone) branchInfo += `\n📞 ${phone}`;            if (hours) branchInfo += `\n🕐 ${hours}`;            return branchInfo;        }));    }    static formatBranchListSimple(branches, includeDetails = false) {        if (!branches || branches.length === 0) {            return '';        }        if (includeDetails) {            return branches.map((b, idx) => {                const address = b.address_detail || '';                const phone = b.phone || '';                const openingHours = b.opening_hours ? `${b.opening_hours}h` : '';                const closeHours = b.close_hours ? `${b.close_hours}h` : '';                const hours = openingHours && closeHours ? `${openingHours} - ${closeHours}` : (openingHours || closeHours || '');                let branchInfo = `${idx + 1}. ${b.name}`;                if (address) branchInfo += `\n   📍 ${address}`;                if (phone) branchInfo += `\n   📞 ${phone}`;                if (hours) branchInfo += `\n   🕐 ${hours}`;                return branchInfo;            }).join('\n\n');        } else {            return branches.map((b, idx) =>                 `${idx + 1}. ${b.name}`            ).join('\n');        }    }}module.exports = BranchFormatter;